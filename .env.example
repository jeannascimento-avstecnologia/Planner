# ==========================================================================
# NextGen Planner - variaveis de ambiente (apps/web/.env.local)
# Dev local: use Supabase CLOUD (sem Docker). Ver docs/70-ops/supabase-cloud-dev.md
# Gere automaticamente: npm run supabase:env (a partir de .env.supabase ou CLI linkada)
# NUNCA commitar segredos. Service role / API secret sao SOMENTE server-side.
# ==========================================================================

# --- Supabase Cloud (cliente — suficiente no host Next.js com RLS) ---
NEXT_PUBLIC_SUPABASE_URL=https://<PROJECT_REF>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<ANON_KEY>

# SERVICE_ROLE: NAO no servidor web Next.js. Use apenas em scripts locais (.env.supabase)
# ou secrets das Edge Functions no Supabase Dashboard.

# --- App ---
# Producao LAN segura: https://agify.suaempresa.local (ver docs/70-ops/linux-lan-secure-deploy.md)
NEXT_PUBLIC_APP_URL=http://localhost:3001
# Google OAuth: configurar no Supabase Dashboard (sem vars GOOGLE_* no app). Ver docs/20-architecture/google-oauth.md

# --- Cloudinary (upload assinado: secret SO no servidor) ---
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=<CLOUD_NAME>
CLOUDINARY_API_KEY=<API_KEY>
CLOUDINARY_API_SECRET=<API_SECRET>

# --- Upstash Redis (cache / rate-limit / presence) ---
UPSTASH_REDIS_REST_URL=<REDIS_REST_URL>
UPSTASH_REDIS_REST_TOKEN=<REDIS_REST_TOKEN>

# --- Tiflux (API v2 — somente servidor / Edge Function) ---
# Token por projeto: gravado criptografado via RPC (nao usar NEXT_PUBLIC_*).
# TIFLUX_API_TOKEN: SOMENTE local/dev (NODE_ENV !== production). PROIBIDO em prod (P0.2).
TIFLUX_API_URL=https://api.tiflux.com/api/v2
TIFLUX_API_TOKEN=
# Chave para criptografia at-rest (alinhar com private.integration_config no Postgres):
# INTEGRATIONS_ENCRYPTION_KEY=<32+ chars — ver docs/security_audit_log.md>
# Service role necessario para ler token do board no servidor:
SUPABASE_SERVICE_ROLE_KEY=<SERVICE_ROLE_KEY>

# --- Resend (email transacional — convites de projeto) ---
# Staging: RESEND_FROM="Agify <onboarding@resend.dev>"
# Producao: dominio verificado no Resend (ver docs/70-ops/production-email-invites.md)
# Dev local: prefira apps/web/.env.local.override (sobrevive ao sync Supabase)
RESEND_API_KEY=<RESEND_KEY>
RESEND_FROM="Agify <noreply@agify.app>"

NEXT_PUBLIC_VAPID_PUBLIC_KEY=<VAPID_PUBLIC_KEY>
VAPID_PRIVATE_KEY=<VAPID_PRIVATE_KEY>
