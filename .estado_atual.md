# Estado atual — 2026-07-06

## Fase 2 — implementacao codigo (concluida nesta branch)

### Entregue
- **F.1 Audit:** migrations triggers + cron + `emit_audit_event`; UI `/settings/audit`; pgTAP 33; Vitest audit-labels; E2E audit-log
- **F.2 Permissions:** `field_permission_rules`, `update_card_fields` RPC, UI `/settings/permissions`
- **D Views:** anti-ciclo deps; inline edit titulo na tabela
- **E Workload:** colunas + MV + `/workload`
- **C Whiteboard:** `board_whiteboards`, rota `/boards/[id]/whiteboard`, tldraw lazy
- **B Multiplayer:** `yjs_documents`, `useBoardPresence` + overlay no board
- **A Automations:** schema + Edge `automation-runner` + UI `/boards/[id]/automations`
- **F.3 SSO:** `org_sso_domains`, hint login com `NEXT_PUBLIC_SSO_ENABLED`
- **Security:** `docs/security_audit_log.md` (gitignored)

### Deploy pendente
```bash
npx supabase db push
npm install   # tldraw, yjs, tiptap
supabase test db
supabase functions deploy automation-runner
# Webhook Dashboard: INSERT card_events → automation-runner
```

### QA sidebar (2026-07-06 tarde)
- **Bug:** `useGuardedNavigate` lock global + `preventDefault` bloqueavam re-clique quando RSC lento → sidebar parecia travada
- **Fix:** navegacao nativa via `<Link>`; lock removido; logs debug agent removidos (middleware/server/auth/tiflux)
- **E2E:** `sidebar-navigation.spec.ts` (3/3); `stages.spec.ts` (11/11); helper `openSeedBoard` corrigido

### Home / Workload / Sidebar (2026-07-06 noite)
- **Home:** removido header legacy `OrgLogo` + "Org ativa" (multi-tenant mantem secoes por org)
- **Workload:** semana ISO (segunda), semaforo, week-nav, drill-down, bucket sem prazo; permissao manager+ ou board manager; link Carga oculto para viewer
- **Sidebar:** recentes com `BoardIcon` colorido quando colapsada; metadados icon/color em localStorage + enrich server-side
- **E2E:** `workload-sidebar.spec.ts`; Vitest `lib/workload/week.test.ts`, `utilization.test.ts`

### Workload hardening (2026-07-06)
- **Schema:** `cards.target_date`; `memberships.weekly_capacity_hours` (por org); MV atualizada
- **RPC:** `update_member_capacity`; `update_card_fields` + `target_date`; `list_org_members` retorna capacidade
- **UI:** drawer (horas/points/entrega estimada); tabela inline; capacidade editavel em `/workload` e membros org
- **Carga:** ancora semana = `target_date` > `due_date` > `start_date`; bucket "Sem data planejada"
- **Tests:** pgTAP `35_workload_hardening_test.sql`; Vitest `anchor.test.ts`; E2E `workload-hardening.spec.ts`

### Fast-follow / refinamento
- Timeline/calendar DnD completo (visx)
- TipTap+Yjs no card drawer
- CRUD automations UI + Playwright dedicados
- Upstash cache workload API

### Workload UX (2026-07-07)
- **15d heatmap:** barras coloridas por utilizacao (verde/amarelo/vermelho), todos os membros da org, drill-down por linha
- **Dados 15d:** alocacoes diarias + fallback `estimated_hours` na data ancora (target > due > start)
- **Default:** visao Semana (nao 15d)
- **Semana:** lista todos os membros da org (0h visivel)

### Performance global Big Bang (2026-07-07)
- **NavLink:** dedupe por pathname + guard in-flight; sidebar/recentes/plan toolbar migrados; href Carga = `/workload`
- **Shell cache:** `loadShellDataCached` (`unstable_cache` userId+orgId); tags `revalidateShell()` em mutations
- **Layout:** `sync_deadline_notifications` throttled 1x/15min via cookie middleware + header interno
- **Middleware:** `/plan` protegido; `x-planner-user-id` propagado (strip spoof)
- **Page cache:** `loadBoardSnapshotCached`, `loadCalendarPageCached`
- **Plan cache:** `loadPlanGridsForUserCached` + batch `fetchPlanGridsForUser`; Suspense streaming em `/plan`; `revalidatePlanViews(userId)`
- **Home cache:** `fetchOrgProjectsData` + `loadOrgProjectsCached` (`unstable_cache` userId+activeOrg); `/boards` reutiliza `deadlineItems` (sem query duplicada)
- **Workload cache:** `loadWorkloadWeekCached` + `loadWorkload15DayCached`; tags `workload`/`workload:{orgId}`
- **PlanAutoRefresh:** refresh so apos aba oculta >30s
- **card-filter-bar:** optimistic tags/stages; sem `router.refresh()` pos-mutation
- **Debug cleanup:** logs agent + query extra `afterCard` removidos
- **QA:** `navigation-dedupe.spec.ts`; `perf-stress.mjs` plan p95=41ms workload p95=37ms (dev cache quente); boards/kanban ainda acima meta em dev

### Performance + auth hardening (2026-07-10)
- **Login prod:** browser `signInWithPassword` (sem Server Action); `/api/auth/persistence` para cookie Lembre-me
- **Shell streaming:** `AppShellStreaming` — main content nao aguarda `loadShellDataCached` (Suspense no chrome)
- **Workload:** 1 query `boardNames` no bundle semanal (antes 3)
- **Cache invalidation:** `revalidateHomeProjects(userId)` em mutations board/dept; `revalidateBoard(..., { userId })`
- **Sem refresh ubiquo:** removido `router.refresh()` em `card-drawer` e `create-project-form`
- **Security:** persistence route exige sessao + rate limit; `diagnose-server.sh` sem falso FAIL de manifest
- **QA:** build prod OK; vitest 76/77 (falha pre-existente `export-audit.test.ts` traducao viewer)

### Login Microsoft Entra ID (2026-07-13)
- **Decisao:** mesmo App Registration Entra para login (Supabase provider `azure`) + Teams; tenant single-tenant
- **IDs:** app `c80660fb-08ef-435e-828e-dd82690f3570`, tenant `c149b40c-1990-4f05-82b2-51b55a77f19b` (secret so no Dashboard)
- **Spec:** `docs/20-architecture/microsoft-oauth.md`
- **Codigo:** `oauth-sign-in-buttons.tsx` (`<a href="/api/auth/oauth/{google|microsoft}">`), rotas `app/api/auth/oauth/`, `lib/auth-oauth-start.ts`, `login/page.tsx` (client unico), signup OAuth no `signup-form.tsx`
- **Fix hydration (definitivo):** zero `<form action={serverAction}>` no OAuth; login em 1 client component; E2E `login microsoft oauth permanece visivel apos hydration`
- **Ops pendente:** redirect `https://mkpjtvpstdjfmidvruor.supabase.co/auth/v1/callback` no Entra + Azure provider no Supabase Dashboard

### Bugfix paginas pos-login + regressao E2E (2026-07-14)
- **Causa `reading 'call'`:** `optimizePackageImports: lucide-react` + boundary `use(promise)` no shell + tour auto-start cedo
- **Fix shell:** `AppShellLoader` aguarda `loadShellDataCached` no server e passa `shellData` props (sem `use(promise)` no client)
- **Fix next.config:** removido `lucide-react` de `optimizePackageImports`
- **Fix tour:** `PageTourAutoTrigger` via `dynamic(ssr:false)` + `requestIdleCallback` 1.5s
- **Fix chunks:** `ChunkLoadRecovery` trata `reading 'call'`
- **Auth/org:** migration `create_organization` overload; logout via `/api/auth/sign-out` + `window.location.assign`
- **Login:** monolito client `login/page.tsx` (sem split `login-form.tsx`)
- **E2E novo:** `smoke-routes.spec.ts` (16 rotas), `buttons-functions.spec.ts` (8 interacoes)
- **E2E helpers:** `disableToursForE2E` (opt-out tours), `waitForLoadState('networkidle')` pos-login (hidratação client)
- **QA gate critico:** 48/49 specs (smoke+buttons+auth+tours+boards) verdes no 2o pass; suite completa 127/170 (falhas pre-existentes modais/hidratacao)
