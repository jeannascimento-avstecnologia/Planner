# Estado atual — 2026-07-03

## Otimizacao pos-stress (concluida)

### Implementado
- Spec: `docs/60-quality/performance-budgets.md`
- Dedupe `loadOrgProjects()` via React `cache()` + single fetch em `/boards`
- Shell: notifications em Suspense (`notifications-loader.tsx`)
- Board page: 2 waves de queries (8 parallel + card_tags/profiles)
- Rate-limit HTTP: 120/min IP, 60/min user + purge Map em `rate-limit.ts`
- `router.refresh()` removido em `column-header`, `card-drawer` (mantido em `create-project-form` para listar novo projeto)
- Script stress: `scripts/perf-stress.mjs`

### Metricas pos-otimizacao (prod `next start :3002`)

| Rota | p95 baseline | p95 carga 10 concurrent |
|------|--------------|-------------------------|
| `/boards` | **99ms** | **39ms** |
| `/boards/[id]` | **189ms** | **37ms** |

Metas (<600ms) **PASS** em producao.

Dev (`next dev :3001`) inflaciona latencia — nao usar para budget.

### Pendente / fast-follow
- `unstable_cache` cross-request: requer loaders sem `cookies()` dentro do cache (usar fetch+tags ou RPC)
- Header auth dedupe (`x-planner-user-id` no request): adiado — quebra cookies se mal aplicado
- E2E `boards.spec.ts`: drift UI (modal criar projeto, `projectTile` vs `projectLink`, strict `Acme Inc`)
- pgTAP: rodar `supabase db reset --local` manualmente

## Entregue iteracao anterior

- Fix duplicacao cards, DnD Kanban, delete card, specs kanban
