# Estado atual — 2026-07-16

## Ciclo atual

**Tree canvas bugs (reopen)** — status: **POST-FIX VERIFY**

### Root causes (Debugger + Searcher 2ª passagem)

| ID | Causa real | Fix |
|----|------------|-----|
| BUG-UI-2 | `Node.filterKey` **não existe** no @xyflow — fix anterior era no-op | `style.opacity` + React `key` de paint; removeu `memo` |
| BUG-UI-1 | BaseEdge `opacity:0` + motion `pointer-events-none` → hit-test morto; sem optimistic `setEdges` | BaseEdge visível; optimistic filter edge; mousedown+click |

### Testes

Vitest tree: 22 passed (`tree-highlight-paint`, parents, kanban, cache, dagre).

### Smoke (humano)

Hard refresh `?view=tree` → filtro texto (data-highlight muda) → selecionar aresta → Remover → toast + aresta some.
