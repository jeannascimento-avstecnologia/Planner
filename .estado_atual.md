# Estado atual — 2026-07-15

## Tarefa ativa (Gerente)

**Objetivo do negocio:** `npm run dev:local` sobe o stack completo em maquina local (macOS/Linux/Windows) — Supabase local (Docker) + migrations/seed + Next.js porta 3001 — sem depender de PowerShell Windows.

**Maquina do solicitante:** darwin arm64. Toolchain detectada neste host:
- Docker instalado (`/usr/local/bin/docker`)
- **PowerShell ausente** (`powershell`/`pwsh` not found) → `npm run dev:local` **quebra hoje**
- PATH sandbox incompleto para node/npm/supabase (validar no shell do usuario com Homebrew/nvm)

## Gap vs docs travados

| Fonte | Diz hoje | Realidade operacional |
|-------|----------|----------------------|
| GUIA_MESTRE §2 + ADR-0002 | Dev frontend = Supabase **Cloud**; nao `supabase start` | `scripts/start-dev.ps1` + `ensure-supabase-local.ps1` ja sobem Docker local |
| `docs/70-ops/local-dev-start.md` | `npm run dev:local` = Supabase local + Next 3001 | Scripts so `.ps1` (Windows); nao roda em macOS |
| README / `environments.md` | Cloud-first, Docker opcional | Contradiz o runbook local-dev-start |
| `package.json` | `dev:local` → `powershell ... start-dev.ps1` | Bloqueia macOS/Linux sem pwsh |

**Decisao de negocio (PENDENTE ADR):** formalizar **dois modos**:
1. `dev:local` — stack full local (Docker + Next) — default para offline / email-senha / mentor
2. `dev:cloud` — Next local + Supabase Cloud — OAuth Microsoft/Google

ADR-0002 / GUIA_MESTRE / environments.md **devem ser atualizados antes do codigo** (guarda-mestra). Sem isso = violacao.

## Escopo "completo" (MVP deste pedido)

Inclui:
- Preflight: Docker Engine, Node >=20, Supabase CLI
- `supabase start` (+ repair volume se PG incompativel)
- `migration up --local` + seed (`admin@nextgen.dev` / `password123`)
- Gerar `apps/web/.env.local` via status local (`127.0.0.1:54321`)
- Limpar portas 3000–3002 + `.next` stale
- Subir `next dev -p 3001`
- Scripts cross-platform (bash primario em darwin/linux; ps1 mantido ou wrapper npm)

Fora / degradado (documentar, nao bloquear):
- OAuth Microsoft/Google → usar `dev:cloud` (ja documentado)
- Upstash Redis / Resend / Cloudinary reais → opcional; rate-limit desliga sem Upstash
- `supabase functions serve` → fast-follow deste epic (automations); nao bloqueia login/Kanban core

## Spec / plano

- Runbook base: `docs/70-ops/local-dev-start.md` (atualizar para bash + macOS)
- Spec ops a criar/atualizar: `docs/70-ops/local-full-stack.md` (Acceptance Criteria)
- ADR a atualizar: `docs/20-architecture/ADR-0002-deployment-topology.md` (amend: local stack permitido em `dev:local`)
- Guia: 1 linha em GUIA_MESTRE §2 paridade ambientes

## Delegacao (ordem unitaria — 1 linguagem/tipo por passo)

| # | Especialista | Entrega | Arquivos (foco) |
|---|--------------|---------|-----------------|
| 0 | @programador (Tech Lead) | Desenho tecnico + AC; confirmar ADR amend | docs ADR/spec apenas |
| 1 | Mentor/docs | Amend ADR-0002 + GUIA + environments + local-dev-start | `docs/**` so markdown |
| 2 | Shell/Ops | Wrapper cross-platform `scripts/start-dev.sh` (+ ensure/sync bash) | `scripts/*.sh` apenas |
| 3 | package.json | `dev:local` detecta OS ou chama `.sh`; manter `.ps1` Win | `package.json` so |
| 4 | Debugger | Validar: `npm run dev:local` → login seed em :3001 | smoke manual |
| 5 | Tester | Checklist E2E smoke login local (opcional) | se houver tempo |

**Regra Gerente:** nao misturar edicao de docs + scripts + package.json na mesma resposta do implementador.

## Status
- [x] Analise Gerente
- [ ] ADR/guia atualizados (bloqueia codigo de comportamento novo)
- [ ] Scripts bash cross-platform
- [ ] package.json apontando wrapper
- [ ] Smoke `dev:local` macOS verde
